using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Test class.
// Maybe we should remove the abstract class later. I am not sure if inheritance will be that useful for this project, and I just suggested
// this hierarchy because the game design document had it in the example.

public class Elephant : BaseAnimal
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public override void PlayAnimation()
    {

    }

    public override void PlaySound()
    {
        
    }
}
